package middleware

// Organization is used to delimit statsd identifiers
const Organization = "bithippie"

// Class is used to delimit statsd identifiers
const Class = "sentinel"

// Count is used to store the numebr of counts per endpoint
const Count = "count"
